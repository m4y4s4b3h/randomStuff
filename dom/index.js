// var header = $('h1');
// header.text("IM HUNGRY");
//
// var firstParagraph = $('.paragraphClass');
// firstParagraph.text("im HUNGRY")
//
// var secondParagraph = $('#paragraphID');
// secondParagraph.text("IM  H U N G R Y!!!!!!!!!!");

var button = $('button');
// var message = $('.message');

button.on("click", getNameAndLogToConsole);

function getNameAndLogToConsole() {
  var email = $('.email').val();
  console.log(`Thank you for signing up, ${email}!`);
  // code to actually present a message to user goes here
  }

var button2 = ${'.button2'}
}

button2.on("click", noodle)

function beNice() {
  var name = ${'name-input'}
  console.log(`Hello ${name} how are you?`)
  if radio = ${.good} {
    console.log(`Glad to here that, ${name}!);
  } else if radio = ${.bad} {
    console.log(`Feel better, ${name}!`);
  }
}
